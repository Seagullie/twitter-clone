<div class="container center-children vertical">

  <h2 class="blue-text">Log in</h2>
  <div class="card" style="width: 35%">
    <div class="card-content">
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
        
          <%# <%= f.input( :remember_me,
                       input_html: {id: 'should_rememberrrrrrrrrrr'},
                       boolean_style: :inline,
                       as: (:boolean if devise_mapping.rememberable?)
                       ) %>
          <%# <label for="should_rememberrrrrrrrrrr"><span>Checkbox label</span></span> %>
          
          <%# the remember me checkbox is not visible, for some reason 
          apparently, cause of checkbox text not being in span
          how do i inject my own code, i wonder

          ugh, i did specify :inline option, and it just doesn't work :c 
          will try to do something with it later
          %>
          <%# <span>Checkbox label</span> %>
        </div>
        <br>
        <div class="form-actions">
          <%= f.button :submit, "Log in" %>
        </div>
      <% end %>
      <br>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
